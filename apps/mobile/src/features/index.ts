// Feature logic that isn't tied to a specific screen file lives here as
// it's extracted (mirrors web features where relevant: farms, produce,
// orders, payments) plus mobile-only flows like Sell Produce. Phase 6's
// farm/produce/sell logic currently lives inline in their app/ screens;
// pull shared pieces out here if/when a second screen needs them.
export {};
