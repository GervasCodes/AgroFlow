// USSD/SMS gateway integration (e.g. Africa's Talking or an equivalent
// Tanzania-licensed provider). Sessions are short-lived (30-180s) --
// pre-compute menu responses, never run long matching queries inline.
export {};
