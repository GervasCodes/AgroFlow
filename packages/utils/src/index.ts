// Shared helper functions used across apps -- keep this small and
// generic (formatting, phone-number normalisation, distance helpers).
// Domain-specific logic belongs in apps/api/src/services, not here.
export {};
