{
  "name": "@agroflow/config",
  "version": "0.1.0",
  "description": "Shared constants: role names, permission keys, supported crops, regions, mobile money providers.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
