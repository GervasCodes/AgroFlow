Place Dockerfiles / docker-compose here once each app has a runnable
build. Suggested separate images: api, web (static build), channels
(ussd/sms/whatsapp can share one lightweight image or be split further).
